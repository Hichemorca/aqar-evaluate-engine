# Public UI Regression Live Verification — 2026-08-26

The production deployment was opened after adding the regression test suite. A live ARJAN evaluation with the verified project `2020 Marquis` produced a visible result.

The result remained visible, contained `Why this valuation?` and `Market indicators`, and did not contain `Range:` or the old `DLD suggestion` wording. The neutral `Verified project suggestion` wording remained visible. This confirms that the regression-protected public presentation is working in production.
