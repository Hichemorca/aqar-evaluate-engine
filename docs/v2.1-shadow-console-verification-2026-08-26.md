# v2.1 Shadow Calibration Console verification — 2026-08-26

The local Calibration Console was opened at `http://127.0.0.1:4173/calibration.html` without entering or exposing an administrator token.

The default calibration source was loaded locally into the page only for rendering verification. The editor rendered all 7 property-type panels, 69 v2.1 shadow controls, and zero project-specific rows under the neutral default configuration. The default shadow configuration was confirmed as present and disabled (`enabled: false`).

The console includes controls for shadow enablement, minimum project evidence, combined bounds, per-type default project multiplier, project-specific multiplier rows, BUA/Plot ratio bands, and renovation-age bands. No remote save was performed and no configuration was changed.
After the one-time global-control fix, the local editor rendered exactly one `Shadow enabled` input and `readEditedConfig()` correctly produced `enabled: true` and an Apartment project multiplier of `1.04` for `apartment | burj khalifa | bahwan tower`. This was a local render test only; no remote configuration was loaded or saved.

The edited shadow configuration serialized successfully to JSON with no `Infinity` values; open-ended ratio and renovation bands were preserved as `null`. This confirms the configuration is safe for versioned storage.

The public-page shadow test used a temporary local-only config for `BAHWAN TOWER` at `1.04×`. With a baseline of AED 1,000,000, the result preserved `finalValue = AED 1,000,000` and produced a separate `shadowValue = AED 1,040,000`; the trace identified the DLD project multiplier. No remote calibration save was performed.

