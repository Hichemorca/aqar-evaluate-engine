# v2.1 Map Project and POI Verification — 2026-08-26

## Project label test

Using the local current build, `DUBAI STUDIO CITY` was selected and the first real DLD project suggestion, `Azizi Mirage 1`, was selected.

| Check | Result |
|---|---|
| DLD suggestion count | 10 |
| Selected project | Azizi Mirage 1 |
| Project label | Visible |
| Project label text | `Azizi Mirage 1 — DLD project name; map point is the current selected marker.` |
| Project map label marker | Present |
| Map point | 25.037, 55.28 |
| Location meaning | Current selected marker; no project coordinates were fabricated |

## Vital facility radius test

The local build queried OSM/Overpass around the current map marker using the active `1000m` radius.

| Check | Result |
|---|---:|
| Facilities returned | 2 |
| POI markers rendered | 2 |
| Maximum returned distance | 963.33m |
| All returned POIs inside radius | Yes |
| Radius context | `Showing 2 vital facilities within 1000m of the current map marker.` |
| Facilities panel | Visible |

The map label is an explicit project label at the current marker, not a fabricated official project centroid. The facility query and renderer both enforce the active radius; each returned POI is filtered by haversine distance before rendering.

## Marker movement and unverified name test

Moving the marker from `25.037, 55.28` to `25.04, 55.29` moved both the project label marker and the POI circle to the new coordinates, while preserving the project label text.

A free-text project name was displayed on the map with the explicit status `User-entered name; not verified and not used to invent a project location.` Clearing the field removed the map label and cleared the selected project state.

## Production verification

The latest production deploy was verified at https://aqar-valuation-engine.netlify.app/.

`DUBAI STUDIO CITY` produced 10 DLD project suggestions; selecting `Azizi Mirage 1` displayed the project label and a project map-label marker. The live radius remained `1000m`. The live Overpass response for that marker returned zero mapped facilities, so the UI correctly displayed `No mapped vital facilities found within 1000m of the current map marker.` This is a data-availability result, not an out-of-radius rendering result; the local positive test confirmed that returned POIs are filtered and rendered inside the radius.
