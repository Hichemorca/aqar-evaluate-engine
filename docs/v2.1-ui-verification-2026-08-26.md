# v2.1 UI verification — 2026-08-26

The local interface was tested from `http://127.0.0.1:4173/index.html`.

- Initial Apartment / Year Built 2020: Project/Building is visible; BUA and Plot Area are hidden; Last Renovation Year is visible because the current year is 2026 and the property age is 6 years.
- Villa / 2020: Project/Building, BUA, Plot Area, and Last Renovation Year are visible.
- Office / 2020: all four v2.1 fields are hidden and previously entered values are cleared.
- The DLD cache loaded 26,766 eligible transactions.
- For Apartment in `BURJ KHALIFA`, the project autocomplete produced real DLD-derived names such as `29 BOULEVARD`, `ACT ONE | ACT TWO`, `BAHWAN TOWER`, `BELLEVUE TOWERS`, and `BLVD CRESCENT`.
- JavaScript syntax and all 53 automated tests passed during this checkpoint.

No production evaluator, Accuracy artifact, calibration, weights, fallback policy, raw DLD file, or historical result was changed by this UI work.
The boundary test with Year Built 2021 hid Last Renovation Year because `2026 - 2021` is exactly five, while the requirement is strictly greater than five. Blank optional values now validate successfully and remain empty strings in the collected payload. Invalid Villa values (`BUA=-1`, `Plot Area=0`, `Last Renovation=2019` with Year Built 2020) were rejected with `bua-must-be-positive`, `plot-area-must-be-positive`, and `renovation-before-construction`.

Reset sets the four v2.1 values to empty and calls `onPropertyTypeChange()`, which re-applies the visibility rules. The shared validation remained isolated from valuation formulas.

For Apartment in `BURJ KHALIFA`, selecting the real DLD suggestion `BAHWAN TOWER` produced `projectBuildingSource = dld-suggestion`, `projectEvidenceCount = 6`, and the notice: `DLD project evidence: 6 transactions. Project data is captured for isolated analysis; current valuation remains district-based.`

Land / 2020 showed only Plot Area; Project/Building, BUA, and Last Renovation Year were hidden. A free-text value `Invented Tower` was marked `user-entered`, had zero verified project evidence, and displayed the visible notice that district evidence remains the fallback and no project price is invented.

After reload, project suggestions were gated by District: before selecting a district the suggestion set was empty; after selecting `BURJ KHALIFA`, it contained 39 Apartment project names, including the DLD-derived examples recorded above.

