# v2.1 Production Deployment Verification — 2026-08-26

The current `feature/v2.1-property-fields` contents were deployed to the existing Netlify site `aqar-valuation-engine` using production context.

| Check | Result |
|---|---|
| Deploy ID | `6a8e5068124360491ddc7931` |
| Deploy state | `ready` |
| Context | `production` |
| Published URL | https://aqar-valuation-engine.netlify.app |
| Function present | `valuation-observation` |
| Netlify secret scan | 127 files scanned; no matches |
| Live optional consent | Visible and unchecked by default |
| Live valuation button | Enabled without consent |
| Live Accuracy header | 8,221 records and 85.2% after page load |

The page loaded successfully in the production browser. The public page displays the v2.1 optional fields and the anonymous observation opt-in. No production observation was submitted during this verification.

The observation endpoint uses the existing `/api/*` redirect and the dedicated `aqar-input-observations` Netlify Blobs namespace. Collection is opt-in and shadow-only; the official evaluator, Accuracy artifact, active weights, fallback policy, and historical results remain outside the observation write path.

## Live endpoint smoke tests

A production POST with an empty payload returned HTTP 400 and the expected validation errors, including `explicit-consent-required`, without writing an observation.

A live browser inspection confirmed:

| Check | Result |
|---|---|
| `window.aqarV21ShadowConfig.enabled` | `false` |
| Observation checkbox default | `false` |
| Observation submit function | Present |
| Active calibration config | `cal-1787651025399` |

No consented sample was submitted during smoke testing, so the production observation store contains no test record from this verification.
